{
  "name": "react_todo_for_yellow_leaft_t",
  "homepage": ".",
  "version": "0.1.0",
  "author": "Khrystyna Yelyseyeva",
  "dependencies": {
    "classnames": "^2.2.6",
    "node-sass": "^4.14.1",
    "prop-types": "^15.7.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint-js": "eslint --ext .jsx,.js ./src/ --fix",
    "lint-css": "stylelint src/**/*.css src/**/*.scss --fix",
    "lint": "npm run lint-js && npm run lint-css",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "lint-staged": {
    "*.css": [
      "stylelint",
      "git add"
    ],
    "*.js": [
      "eslint",
      "git add"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@mate-academy/eslint-config-react": "0.0.5",
    "@mate-academy/stylelint-config": "*",
    "eslint": "^7.13.0",
    "gh-pages": "^2.2.0",
    "lint-staged": "^9.5.0",
    "stylelint": "^13.0.0"
  }
}
